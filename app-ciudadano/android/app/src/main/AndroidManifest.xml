<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Sin esto, la app de release no tiene red. Flutter la declara solo en los manifiestos de debug y
      de profile, así que una app que habla con un laboratorio y se compila en release se queda muda
      sin un solo mensaje de error que lo explique.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="HispaLIS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Tema que se ve mientras arranca el motor de Flutter. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
          La puerta por la que el navegador devuelve el control a la app al terminar la autorización
          SMART. El esquema tiene que ser EXACTAMENTE el de `ConfiguracionSmart.esquemaPropio`: si no
          coinciden, el navegador se queda con la redirección, la app no se entera y el flujo muere
          en una pestaña abierta sin ningún error.

          `taskAffinity=""` la mantiene en la misma tarea que la app; sin eso, volver de la
          autorización abre una segunda instancia y la sesión se pierde por el camino.
        -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="es.hispalis.ciudadano" />
            </intent-filter>
        </activity>

        <!-- No borrar: de aquí sale GeneratedPluginRegistrant.java. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!--
      Lo usa el motor de Flutter para saber qué apps pueden procesar texto seleccionado.
      https://developer.android.com/training/package-visibility
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
